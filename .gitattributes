# Auto detect text files and perform LF normalization
* text=auto

# Batch files must keep CRLF or Windows can mis-parse labels and goto
*.bat text eol=crlf

# Never normalise binaries
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.webp binary
